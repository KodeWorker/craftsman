# 2026-5-8

## Phase 5

## Done
* put `plan tools` into todo in roadmap.md
* implement browser tools

## TODO
- memory tool (own, unowned)
- `search_tools` should be revised with vector db

## Browser Tools Integration Test

Enable browser tools first: set `tools.browser.enabled: true` in `craftsman.yaml`, then `uv run playwright install chromium`.

- [x] `browser:navigate` — "navigate to https://example.com"
- [x] `browser:get_text` — "get the text of the current page"
- [x] `browser:aria_snapshot` — "show me the accessibility tree of the current page"
- [x] `browser:click` — "click the 'More information...' link on the page"
- [x] `browser:type` — "navigate to https://www.google.com then type 'playwright python' in the search box"
- [x] `browser:wait` — "wait 2000 milliseconds"
- [x] `browser:scroll` — "scroll down 500 pixels"
- [x] `browser:hover` — "hover over the first link on the page"
- [x] `browser:select` — "navigate to https://the-internet.herokuapp.com/dropdown and select Option 2"
- [x] `browser:screenshot` — "take a screenshot of the current page"
- [x] `browser:eval` — "navigate to https://example.com then run the JavaScript expression `document.title` and tell me the result"
