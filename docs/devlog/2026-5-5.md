# 2026-5-5

## Phase 5
- design change: plan tool should be revised in later phase

## Done
- deferred `plan:*` / `task:*` tools from registry and executor:
    - LLM prefers `agent:run` over individual task tool calls → human
      verification bypassed for each step
    - `task:verify` has no ground truth — agent self-reports done via
      `task:done` without any objective check
    - `plan_tools.py` kept but not registered; revisit in a later phase
      once a verification oracle design is settled

## TODO
- update unit tests
- test telegram tools
- manual test on each tool
- memory tool (own, unowned)
- `search_tools` should be revised with vector db
