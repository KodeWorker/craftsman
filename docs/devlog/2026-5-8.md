# 2026-5-8

## Phase 5

## Done
* fix `bash:run` with multi cmd: `cd path/to/go && do-something`, drop `shlex.split` and use executable="/bin/bash"
* max tool calls: inform user for every `tool_loop_checkpoint` if continue
* restart client should reload provider.model

## TODO
- plan tool revise -> (research only no task involved)
- memory tool (own, unowned)
- `search_tools` should be revised with vector db
