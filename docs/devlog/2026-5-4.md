# 2026-5-4

## Phase 5
- design change: add `agent:run` tool
- design change: JobDispatcher should live on client-side (tool use)
- design change: add `powershell:run` (sys.platform aware)

## Done
- bash:* not working for windows -> use Python native
- empty tool result problem (root cause): no default system prompt
- text:replace problem: text:read returns `l.rstrip("\n")` which preserves trailing spaces
- modified tool seeding process
- ~~cost manager cli tool~~ (add `/cost` for chat and telegram)
- ~~cron/schedule control cli~~ (`craftsman daemon`)

## Manual Tool Tests

**meta**
- [ ] `tool:list` — "list all available tools"
- [ ] `tool:describe` — "describe the bash:grep tool"
- [ ] `tool:find` — "find a tool that can search file contents"
- [ ] `tool:revoke` — "revoke bash:du so it's no longer available"

**bash**
- [ ] `bash:ls` — "list files in the current directory"
- [ ] `bash:cat` — "show me the contents of README.md"
- [ ] `bash:grep` — "search for 'def chat' in src/craftsman/cli.py"
- [ ] `bash:find` — "find all .py files under src/"
- [ ] `bash:head` — "show the first 10 lines of README.md"
- [ ] `bash:tail` — "show the last 10 lines of README.md"
- [ ] `bash:stat` — "show file info for README.md"
- [ ] `bash:ps` — "list running processes"
- [ ] `bash:df` — "show disk usage for the current drive"
- [ ] `bash:du` — "show directory sizes under src/"
- [ ] `powershell:run` — "run Get-Date in PowerShell" *(Windows only)*

**text**
- [ ] `text:read` — "read lines 1-30 of src/craftsman/cli.py"
- [ ] `text:search` — "search for 'import' in src/craftsman/server.py"
- [ ] `text:replace` — "in dummy.py replace 'hello' with 'world'"
- [ ] `text:insert` — "insert a comment '# TODO' at line 1 of dummy.py"
- [ ] `text:delete` — "delete lines 1-2 of dummy.py"

**memory**
- [ ] `memory:store` — "remember that my preferred language is Python"
- [ ] `memory:retrieve` — "what have you stored about my preferences?"
- [ ] `memory:forget` — "forget the fact about my language preference"

**schedule**
- [ ] `schedule:at` — "in 2 minutes run bash:ls on the current directory"
- [ ] `schedule:list` — "list all scheduled jobs"
- [ ] `schedule:cancel` — "cancel scheduled job [id]"
- [ ] `cron:create` — "every minute list files in the current directory"
- [ ] `cron:list` — "list all cron jobs"
- [ ] `cron:remove` — "remove cron job [id]"

**plan / task**
- [ ] `plan:create` — "create a plan to refactor the auth module"
- [ ] `task:create` — "add a task to update imports under plan [id]"
- [ ] `task:start` — "start task [id]"
- [ ] `task:verify` — "mark task [id] as verifying"
- [ ] `task:done` — "mark task [id] as done with output 'imports updated'"
- [ ] `task:fail` — "mark task [id] as failed, reason: blocked by missing dep"
- [ ] `task:list` — "list all tasks in plan [id]"
- [ ] `plan:done` — "mark plan [id] as done"

**agent**
- [ ] `agent:run` — "use a subagent to count all .py files under src/ and report the total"

## TODO
- manual test on each tool
- memory tool (own, unowned)
