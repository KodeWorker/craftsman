# 2026-5-4

## Phase 5
- design change: add `agent:run` tool
- design change: JobDispatcher should live on client-side (tool use)
- design change: add `powershell:run` (sys.platform aware)

## Done
- bash:* not working for windows -> use Python native
- empty tool result problem (root cause): no default system prompt
- text:replace problem: text:read returns `l.rstrip("\n")` which preserves trailing spaces
- modified tool seeding process
- ~~cost manager cli tool~~ (add `/cost` for chat and telegram)
- ~~cron/schedule control cli~~ (`craftsman daemon`)
- async chat/telefram to enable agent loop with cron/scheduled job

## Manual Tool Tests

**meta**
- [x] `tool:list` — "list all available tools"
- [x] `tool:describe` — "describe the bash:grep tool"
- [x] `tool:find` — "find a tool that can search file contents"
- [x] `tool:revoke` — "revoke bash:du so it's no longer available"

**bash**
- [x] `bash:ls` — "list files in the current directory"
- [x] `bash:cat` — "show me the contents of README.md"
- [x] `bash:grep` — "search for 'def chat' in src/craftsman/cli.py"
- [x] `bash:find` — "find all .py files under src/"
- [x] `bash:head` — "show the first 10 lines of README.md"
- [x] `bash:tail` — "show the last 10 lines of README.md"
- [x] `bash:stat` — "show file info for README.md"
- [x] `bash:ps` — "list running processes"
- [x] `bash:df` — "show disk usage for the current drive"
- [x] `bash:du` — "show directory sizes under src/"
- [x] `powershell:run` — "run Get-Date in PowerShell" *(Windows only)*

**text**
- [x] `text:read` — "read lines 1-30 of src/craftsman/cli.py"
- [x] `text:search` — "search for 'import' in src/craftsman/server.py"
- [x] `text:replace` — "in dummy.py replace 'hello' with 'world'"
- [x] `text:insert` — "insert a comment '# TODO' at line 1 of dummy.py"
- [x] `text:delete` — "delete lines 1-2 of dummy.py"

**memory**
- [x] `memory:store` — "remember that my preferred language is Python"
- [x] `memory:retrieve` — "what have you stored about my preferences?"
- [x] `memory:forget` — "forget the fact about my language preference"

**schedule**
- [ ] `schedule:at` — "in 2 minutes run bash:ls on the current directory"
- [x] `schedule:list` — "list all scheduled jobs"
- [x] `schedule:cancel` — "cancel scheduled job [id]"
- [ ] `cron:create` — "every minute list files in the current directory"
- [ ] `cron:list` — "list all cron jobs"
- [ ] `cron:remove` — "remove cron job [id]"

**plan / task**
- [ ] `plan:create` — "create a plan to refactor the auth module"
- [ ] `task:create` — "add a task to update imports under plan [id]"
- [ ] `task:start` — "start task [id]"
- [ ] `task:verify` — "mark task [id] as verifying"
- [ ] `task:done` — "mark task [id] as done with output 'imports updated'"
- [ ] `task:fail` — "mark task [id] as failed, reason: blocked by missing dep"
- [ ] `task:list` — "list all tasks in plan [id]"
- [ ] `plan:done` — "mark plan [id] as done"

**agent**
- [ ] `agent:run` — "use a subagent to count all .py files under src/ and report the total"

## TODO
- job consumed silently
- update unit tests
- test telegram tools
- manual test on each tool
- memory tool (own, unowned)
- `search_tools` should be revised with verctor db
